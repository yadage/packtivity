process:
  process_type: 'interpolated-script-cmd'
  script: 'echo Hello World > {outputfile}'
publisher:
  publisher_type: 'frompar-pub'
  outputmap:
    output: outputfile
environment:
  environment_type: 'docker-encapsulated'
  image: busybox